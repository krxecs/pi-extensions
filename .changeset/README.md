# Release intents

This directory contains release intents in the Changesets markdown format.
pnpm consumes them natively with `pnpm version -r` and publishes versioned
workspace packages with `pnpm publish -r`.

See the [pnpm release management documentation](https://pnpm.io/versioning)
for the workflow.
